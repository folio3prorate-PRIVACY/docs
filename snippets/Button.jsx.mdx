export default function Button({ href, children }) {
  return (
    <a href={href} className="inline-block rounded bg-blue-600 px-4 py-2 font-medium text-white hover:bg-blue-700">
      {children}
    </a>
  );
}